{% extends 'base.html.twig' %}

{% block title %}ReviewAssignment{% endblock %}

{% block body %}
    <h1>ReviewAssignment</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ review_assignment.id }}</td>
            </tr>
            <tr>
                <th>Duedate</th>
                <td>{{ review_assignment.duedate ? review_assignment.duedate|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('review_assignment_index') }}">back to list</a>

    <a href="{{ path('review_assignment_edit', {'id': review_assignment.id}) }}">edit</a>

    {{ include('review_assignment/_delete_form.html.twig') }}
{% endblock %}
