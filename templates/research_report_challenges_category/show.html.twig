{% extends 'base.html.twig' %}

{% block title %}ResearchReportChallengesCategory{% endblock %}

{% block body %}
    <h1>ResearchReportChallengesCategory</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ research_report_challenges_category.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ research_report_challenges_category.name }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ research_report_challenges_category.description }}</td>
            </tr>
            <tr>
                <th>CreatedAt</th>
                <td>{{ research_report_challenges_category.createdAt ? research_report_challenges_category.createdAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>IsActive</th>
                <td>{{ research_report_challenges_category.isActive ? 'Yes' : 'No' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('research_report_challenges_category_index') }}">back to list</a>

    <a href="{{ path('research_report_challenges_category_edit', {'id': research_report_challenges_category.id}) }}">edit</a>

    {{ include('research_report_challenges_category/_delete_form.html.twig') }}
{% endblock %}
