{% extends 'base.html.twig' %}

{% block title %}ProjectAttachmentType{% endblock %}

{% block body %}
    <h1>ProjectAttachmentType</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ project_attachment_type.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ project_attachment_type.name }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ project_attachment_type.description }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_c_r_p_project_attachment_type_index') }}">back to list</a>

    <a href="{{ path('app_c_r_p_project_attachment_type_edit', {'id': project_attachment_type.id}) }}">edit</a>

    {{ include('crp/project_attachment_type/_delete_form.html.twig') }}
{% endblock %}
