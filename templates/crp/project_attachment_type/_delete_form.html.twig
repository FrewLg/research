<form method="post" action="{{ path('app_c_r_p_project_attachment_type_delete', {'id': project_attachment_type.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ project_attachment_type.id) }}">
    <button class="btn">Delete</button>
</form>
