{% extends 'base.html.twig' %}

{% block title %}ProjectProgress{% endblock %}

{% block body %}
    <h1>ProjectProgress</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ project_progress.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ project_progress.name }}</td>
            </tr>
            <tr>
                <th>Progress</th>
                <td>{{ project_progress.progress }}</td>
            </tr>
            <tr>
                <th>ProgressColor</th>
                <td>{{ project_progress.progressColor }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_c_r_p_project_progress_index') }}">back to list</a>

    <a href="{{ path('app_c_r_p_project_progress_edit', {'id': project_progress.id}) }}">edit</a>

    {{ include('crp/project_progress/_delete_form.html.twig') }}
{% endblock %}
