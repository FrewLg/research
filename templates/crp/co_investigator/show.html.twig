{% extends 'base.html.twig' %}

{% block title %}CoInvestigator{% endblock %}

{% block body %}
    <h1>CoInvestigator</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ co_investigator.id }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_c_r_p_co_investigator_index') }}">back to list</a>

    <a href="{{ path('app_c_r_p_co_investigator_edit', {'id': co_investigator.id}) }}">edit</a>

    {{ include('crp/co_investigator/_delete_form.html.twig') }}
{% endblock %}
