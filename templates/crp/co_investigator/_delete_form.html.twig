<form method="post" action="{{ path('app_c_r_p_co_investigator_delete', {'id': co_investigator.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ co_investigator.id) }}">
    <button class="btn">Delete</button>
</form>
