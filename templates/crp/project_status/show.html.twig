{% extends 'base.html.twig' %}

{% block title %}ProjectStatus{% endblock %}

{% block body %}
    <h1>ProjectStatus</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ project_status.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ project_status.name }}</td>
            </tr>
            <tr>
                <th>Status</th>
                <td>{{ project_status.status }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ project_status.description }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_c_r_p_project_status_index') }}">back to list</a>

    <a href="{{ path('app_c_r_p_project_status_edit', {'id': project_status.id}) }}">edit</a>

    {{ include('crp/project_status/_delete_form.html.twig') }}
{% endblock %}
