<form method="post" action="{{ path('template_and_form_delete', {'id': template_and_form.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ template_and_form.id) }}">
    <button class="btn btn-sm btn-danger">Delete</button>
</form>
