<div
	class="left-sidenav">
	<!-- LOGO -->
	<div class="brand">
		<a href="{{ path('homepage') }}" class="logo">
			<span>
				<img src="{{ asset('files/site_setting/'~site_logo ) }}" alt="" class="logo-sm">
			</span>
			<span>
				<!--   <img src="{{ asset('files/site_setting/'~site_logo ) }}" alt="logo-large" class="logo-lg logo-light"> <img src="{{ asset('files/site_setting/'~site_logo ) }}" alt="logo-large" class="logo-lg logo-dark">
																												      </span> -->
			</a>
		</div>

		<!--end logo-->
		<div class="menu-content h-100" data-simplebar="data-simplebar">
			<ul class="metismenu left-sidenav-menu">
				<li class="menu-label mt-0">{{ app_name}}</li>

				{% if is_granted("view_dashboard") %}
					<li>
						<a href="{{ path('dashboard') }}">
							<i class=" fas fa-home align-self-center menu-icon"></i>
							<span>Dashboard
							</span>
						</a>

					</li>
				{% endif %}
				{% include "includes/menus/submission.html.twig" %}
				{% include "includes/menus/researchs.html.twig" %}
				{% include "includes/menus/irb.html.twig" %}
				{% include "includes/menus/oportunities.html.twig" %}
				{% include "includes/menus/user.html.twig" %}
				{% include "includes/menus/workunit.html.twig" %}
				{% include "includes/menus/services.html.twig" %}
				{% include "includes/menus/settings.html.twig" %}
				{% include "includes/menus/reports.html.twig" %}


			</ul>

			<div class="update-msg text-center">
				<a href="javascript: void(0);" class="float-end close-btn text-muted" data-bs-dismiss="update-msg" aria-label="Close" aria-hidden="true">
					<i class="mdi mdi-close"></i>
				</a>
				<h5 class="mt-3">{{ app_name|raw}}</h5>
				<p class="mb-3">
					{{ app_description|raw}}</p>
				<a href="{{path('homepage')}}" class="btn btn-outline-primary btn-sm">Homepage</a>
			</div>
		</div>
	</div>
