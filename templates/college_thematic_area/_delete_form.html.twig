<form method="post" action="{{ path('app_college_thematic_area_delete', {'id': college_thematic_area.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ college_thematic_area.id) }}">
    <button class="btn">Delete</button>
</form>
