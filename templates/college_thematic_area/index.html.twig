{% extends 'base.html.twig' %}

{% block title %}CollegeThematicArea index{% endblock %}

{% block body %}
    <h1>CollegeThematicArea index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Name</th>
                <th>Descryption</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for college_thematic_area in college_thematic_areas %}
            <tr>
                <td>{{ college_thematic_area.id }}</td>
                <td>{{ college_thematic_area.name }}</td>
                <td>{{ college_thematic_area.descryption }}</td>
                <td>
                    <a href="{{ path('app_college_thematic_area_show', {'id': college_thematic_area.id}) }}">show</a>
                    <a href="{{ path('app_college_thematic_area_edit', {'id': college_thematic_area.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="4">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_college_thematic_area_new') }}">Create new</a>
{% endblock %}
