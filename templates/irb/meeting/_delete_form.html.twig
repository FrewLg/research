<form method="post" action="{{ path('i_r_b_meeting_delete', {'id': meeting.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ meeting.id) }}">
    <button class="btn float-end btn-danger">Delete</button>
</form>
