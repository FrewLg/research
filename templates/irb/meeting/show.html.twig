{% extends 'base.html.twig' %}

{% block title %}Meeting{% endblock %}

{% block body %}
    <h1>Meeting</h1>

    <table class="table">
        <tbody>
           
            <tr>
                <th>Number</th>
                <td>{{ meeting.number }}</td>
            </tr>
            <tr>
                <th>meeting date</th>
                <td>{{ meeting.heldAt ? meeting.heldAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('i_r_b_meeting_index') }}">back to list</a>

    <a href="{{ path('i_r_b_meeting_edit', {'id': meeting.id}) }}">edit</a>

    {{ include('irb/meeting/_delete_form.html.twig') }}
{% endblock %}
