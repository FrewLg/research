<form method="post" action="{{ path('i_r_b_review_checklist_delete', {'id': review_checklist.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ review_checklist.id) }}">
    <button class="btn">Delete</button>
</form>
