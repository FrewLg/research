<form method="post" action="{{ path('i_r_b_research_subject_category_delete', {'id': research_subject_category.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ research_subject_category.id) }}">
    <button class="btn btn-danger mt-1">Delete</button>
</form>
