{% extends 'base.html.twig' %}

{% block title %}ResearchSubjectCategory{% endblock %}

{% block body %}
    <h1>ResearchSubjectCategory</h1>

    <div class="card p-1"><table class="table">
        <tbody>
            <tr>
                <th>#</th>
                <td>{{ research_subject_category.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ research_subject_category.name }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ research_subject_category.description }}</td>
            </tr>
        </tbody>
    </table></div>

    <a href="{{ path('i_r_b_research_subject_category_index') }}" class="mt-1 btn btn-primary">back to list</a>

    <a href="{{ path('i_r_b_research_subject_category_edit', {'id': research_subject_category.id}) }}" class="btn btn-warning">edit</a>

    {{ include('irb/research_subject_category/_delete_form.html.twig') }}
{% endblock %}
