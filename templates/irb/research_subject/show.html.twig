{% extends 'base.html.twig' %}

{% block title %}ResearchSubject{% endblock %}

{% block body %}
    <h1>ResearchSubject</h1>

    <div class="card p-1"><table class="table">
        <tbody>
            <tr>
                <th>#</th>
                <td>{{ research_subject.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ research_subject.name }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ research_subject.description }}</td>
            </tr>
            <tr>
                <th>Type</th>
                <td>{{ research_subject.type }}</td>
            </tr>
        </tbody>
    </table></div>

    <a href="{{ path('i_r_b_research_subject_index') }}" class="mt-1 btn btn-primary">back to list</a>

    <a href="{{ path('i_r_b_research_subject_edit', {'id': research_subject.id}) }}" class="btn btn-warning">edit</a>

    {{ include('irb/research_subject/_delete_form.html.twig') }}
{% endblock %}
