{% extends 'base.html.twig' %}

{% block title %}ProjectType{% endblock %}

{% block body %}
    <h1>ProjectType</h1>

    <div class="card"><table class="table">
        <tbody>
            <tr>
                <th>#</th>
                <td>{{ project_type.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ project_type.name }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ project_type.description }}</td>
            </tr>
        </tbody>
    </table>
</div>

    <a href="{{ path('i_r_b_project_type_index') }}" class="mt-1 btn btn-primary">back to list</a>

    <a href="{{ path('i_r_b_project_type_edit', {'id': project_type.id}) }}" class="btn btn-warning">edit</a>

    {{ include('irb/project_type/_delete_form.html.twig') }}
{% endblock %}
