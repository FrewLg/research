{% extends 'base.html.twig' %}

{% block title %}ReviewChecklistGroup index{% endblock %}

{% block body %}
    <h1>ReviewChecklistGroup index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Name</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for review_checklist_group in review_checklist_groups %}
            <tr>
                <td>{{ review_checklist_group.id }}</td>
                <td>{{ review_checklist_group.name }}</td>
                <td>
                    <a href="{{ path('i_r_b_review_checklist_group_show', {'id': review_checklist_group.id}) }}">show</a>
                    <a href="{{ path('i_r_b_review_checklist_group_edit', {'id': review_checklist_group.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('i_r_b_review_checklist_group_new') }}">Create new</a>
{% endblock %}
