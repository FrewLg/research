{% extends 'base.html.twig' %}

{% block title %}ReviewChecklistGroup{% endblock %}

{% block body %}
    <h1>ReviewChecklistGroup</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ review_checklist_group.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ review_checklist_group.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('i_r_b_review_checklist_group_index') }}">back to list</a>

    <a href="{{ path('i_r_b_review_checklist_group_edit', {'id': review_checklist_group.id}) }}">edit</a>

    {{ include('irb/review_checklist_group/_delete_form.html.twig') }}
{% endblock %}
