<form method="post" action="{{ path('i_r_b_review_checklist_group_delete', {'id': review_checklist_group.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ review_checklist_group.id) }}">
    <button class="btn">Delete</button>
</form>
