{% extends 'base.html.twig' %}

{% block title %}AttachmentType{% endblock %}

{% block body %}
    <h1>AttachmentType</h1>

    <div class="card"><table class="table">
        <tbody>
            <tr>
                <th>#</th>
                <td>{{ attachment_type.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ attachment_type.name }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ attachment_type.description }}</td>
            </tr>
            <tr>
                <th>Type</th>
                <td>{{ attachment_type.type }}</td>
            </tr>
        </tbody>
    </table>
</div>

    <a href="{{ path('i_r_b_attachment_type_index') }}" class="mt-1 btn btn-primary">back to list</a>

    <a href="{{ path('i_r_b_attachment_type_edit', {'id': attachment_type.id}) }}" class="btn btn-warning">edit</a>

    {{ include('irb/attachment_type/_delete_form.html.twig') }}
{% endblock %}
