<form method="post" action="{{ path('app_funding_scheme_delete', {'id': funding_scheme.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ funding_scheme.id) }}">
    <button class="btn">Delete</button>
</form>
