{% extends 'base.html.twig' %}

{% block title %}FundingScheme{% endblock %}

{% block body %}
    <h1>FundingScheme</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ funding_scheme.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ funding_scheme.name }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ funding_scheme.description }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_funding_scheme_index') }}">back to list</a>

    <a href="{{ path('app_funding_scheme_edit', {'id': funding_scheme.id}) }}">edit</a>

    {{ include('funding_scheme/_delete_form.html.twig') }}
{% endblock %}
